import { registerRootComponent } from 'expo';
import App from './App';

// Registra el componente raíz para Expo Go
registerRootComponent(App);

// Exportación por defecto requerida por el empaquetador / Expo Snack
export default App;